# Max Number in Array (Basic Algorithm)

**Goal:** Find the largest number in an array.

**Logic:**
- Start with the first number as `max`
- Loop through each number
- If any number is greater than `max`, update it
- Return the final `max`


